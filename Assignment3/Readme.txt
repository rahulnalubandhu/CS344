Steps to compile:
Makesure to download every file in one directory

executable name: smallsh

Enter "gcc -o smallsh smallsh.c" line into the terminal to compile the small shell program 

Then run ./smallsh